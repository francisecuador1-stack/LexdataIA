/** Loads corpus YAML into the database (idempotent by codigo + version). */
// TODO: Implement with Prisma client when DB is available
console.log('Ingest script: TODO — will load corpus to DB idempotently');
console.log('Run `pnpm --filter legal-corpus hash` first to generate corpus.lock.json');
